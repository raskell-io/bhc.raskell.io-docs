+++
title = "Compilation Targets"
description = "BHC compilation targets for different platforms"
sort_by = "weight"
+++

BHC supports multiple compilation targets:

- **Native** - x86_64, aarch64 for Linux, macOS, Windows
- **WASM** - WebAssembly via WASI
